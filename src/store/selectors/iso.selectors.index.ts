import * as base from './iso.selectors.base';
import * as components from './iso.selectors.components';
import * as dots from './iso.selectors.dots';
import * as layers from './iso.selectors.layers';
import * as playback from './iso.selectors.playback';
import * as pos from './iso.selectors.pos';
import * as raw from './iso.selectors.raw';
import * as singles from './iso.selectors.singles';

export const selectors = {
  raw,
  base,
  components,
  dots,
  layers,
  pos,
  playback,
  singles,
};
